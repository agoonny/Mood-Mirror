# Emotion Universe

An experimental generative art installation built with Next.js, React Three Fiber, GLSL shaders, Framer Motion, and Tone.js.

## Run

```bash
npm install
npm run dev
```

Optionally set `OPENAI_API_KEY` to enable API-backed emotion extraction fallback.
